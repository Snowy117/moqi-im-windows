/* This file is left blank on purpose: the ARM64 and ARM64EC object files built
 * from it only exist to let the linker generate ARM64X thunks for the
 * forwarder DLL (same trick as rime/weasel arm64x_wrapper/dummy.c). */
